{
  "name": "@qorium/judge0-orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QOrium Judge0 sandbox orchestrator. PM2 fork-mode worker per infra/Judge0-Sandbox-Integration-Spec-v0.md.",
  "main": "./dist/index.js",
  "bin": {
    "qorium-judge0-orchestrator": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx watch src/cli.ts --once",
    "start": "node dist/cli.js --once",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@qorium/db": "workspace:*",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
