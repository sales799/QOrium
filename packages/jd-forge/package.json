{
  "name": "@qorium/jd-forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JD-Forge SKU 2 alpha — deterministic library pieces (JD-parse schema, role-graph mapper, generation spec builder, output exporters). Live LLM call is cred-bound and lives in services/jd-forge (deferred to cred-drop). Per infra/JD-Forge-v0-Design.md + Constitution §1.2 SKU 2.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
