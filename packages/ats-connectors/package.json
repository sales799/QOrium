{
  "name": "@qorium/ats-connectors",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ATS Connector Framework v0 alpha — universal Candidate/Job/AssessmentResult abstraction, ConnectorAdapter interface, SyncStateMachine, field-mapper, plus a Lever adapter stub. Per ATS-Connector-Framework-v0.md §2. Live fetch + Greenhouse/Workday/Ashby/Darwinbox adapters land in Sprint 4.6.1+.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
