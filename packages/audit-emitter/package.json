{
  "name": "@qorium/audit-emitter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared library that domain services use to emit audit events to @qorium/audit-log with idempotency keys + a typed event taxonomy.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
