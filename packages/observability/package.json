{
  "name": "@qorium/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QOrium observability shims: Sentry SDK wrapper, OpenTelemetry init, Pino → Loki shipper. v0 ships Stub-vs-Real wrappers for each.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
