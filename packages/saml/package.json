{
  "name": "@qorium/saml",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SAML 2.0 + SCIM 2.0 + OIDC primitives: assertion validation, signed SAMLResponse parsing, AuthnRequest builder, JIT provisioning, replay-attack guard, SCIM token validator. Sprint 3.3 per infra/SSO-SAML-Enterprise-Spec-v1.md. Live IdP integration tests halt on cred-drop per spec §9.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.json --force",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "xml-crypto": "^6.1.2"
  }
}
