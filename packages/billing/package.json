{
  "name": "@qorium/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure-TS pricing + GST + HSN/SAC library. SKU pricing tables, INR/USD support, GST calculation (CGST/SGST intra-state and IGST inter-state), GSTIN validation, HSN/SAC code mapping for SaaS. Sprint 2.2 per Auto-Mode Remote Plan §4 Phase D.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
