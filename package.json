{
  "name": "qorium",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QOrium — role-graph-native Question-Bank-as-a-Service",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "typecheck": "pnpm run build:packages && pnpm -r --if-present run typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "pnpm run build:packages && pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build",
    "deploy": "/usr/local/bin/safe-deploy qorium-marketing",
    "deploy:atomic": "/usr/local/bin/safe-deploy qorium-marketing",
    "deploy:atomic:raw": "TALPRO_GBS_RAW_DEPLOY=1 bash infra/marketing-deploy.sh",
    "dev:marketing": "pnpm --filter @qorium/marketing dev",
    "secrets:scan": "gitleaks detect --source . --no-banner --redact || (echo 'gitleaks not installed — see README' && exit 1)",
    "prepare": "husky",
    "build:packages": "pnpm --filter @qorium/db --filter @qorium/auth --filter @qorium/saml run build",
    "ops:validate-credentials": "node scripts/validate-credentials.mjs",
    "ops:validate-credentials:strict": "node scripts/validate-credentials.mjs --strict",
    "ops:validate-credentials:json": "node scripts/validate-credentials.mjs --json",
    "ops:validate-credentials:test": "node --test scripts/validate-credentials.test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.9.1",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "husky": "^9.1.6",
    "lint-staged": "^17.0.7",
    "prettier": "^3.3.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.60.1"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs}": [
      "prettier --write",
      "eslint --fix --config eslint.config.js --no-config-lookup --no-warn-ignored"
    ],
    "*.{json,md,mdx,yml,yaml,css}": [
      "prettier --write"
    ]
  },
  "pnpm": {
    "overrides": {
      "brace-expansion": "^5.0.6",
      "esbuild": "^0.25.0",
      "postcss": "^8.5.10",
      "qs": "^6.15.2",
      "vite": "^6.4.2"
    }
  }
}
